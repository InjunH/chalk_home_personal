# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 언어 설정 (Language Setting)

**중요**: 이 프로젝트에서는 한국어로 대화해주세요. 모든 설명, 주석, 커밋 메시지는 한국어를 사용합니다.

## Project Overview

This is a new repository called `chalk_home` that is currently being initialized. As the codebase develops, this file should be updated with:

## Development Setup

Since this is a new project, the specific setup instructions will depend on the technology stack chosen. When implementing the project:

1. Choose an appropriate technology stack based on project requirements
2. Initialize the project with the chosen framework/language package manager
3. Set up linting and formatting tools appropriate for the chosen stack
4. Configure testing framework

## Architecture Guidelines

As the project develops, document:

- Core architectural decisions and patterns
- Key dependencies and their purposes
- Important technical constraints or requirements

## Common Commands

Commands will be added as the project structure is established. Typical commands to document include:

- Installation/setup commands
- Development server commands
- Build/compilation commands
- Test execution commands
- Deployment procedures

## Notes

This is a newly initialized repository with no existing code structure. When developing features:

- Consider the project name "chalk_home" which suggests it may be related to a home/dashboard application
- Establish clear project structure from the beginning
- Document architectural decisions as they are made
